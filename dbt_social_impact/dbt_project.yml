name: 'dbt_social_impact'
version: '1.0.0'

profile: 'dbt_social_impact'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_social_impact:
    staging:
      +materialized: view
    marts:
      +materialized: view